node_modules/
.next/
dist/
.env
.env.local
*.log
.DS_Store
.turbo/
.nx/

# Large binary files (ONNX models, WASM) — serve from CDN
*.wasm
*.onnx
*.onnx.metadata
*.onnx.lock

# IDE / workspace
*.code-workspace

# Temp files
.git-commit-msg.txt

# macOS duplicate files (Finder copy artifacts)
*\ 2.*
*\ 3.*

# Build artifacts
*.tsbuildinfo

# Scripts — compiled app bundles
scripts/*.app/
